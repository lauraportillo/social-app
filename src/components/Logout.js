// React
import { Link } from 'react-router-dom';
// Styles
import '../stylesheets/Logout.scss';

const Logout = (props) => {
  return (
    <>
      <div>LOGOUT</div>
    </>
  );
};

export default Logout;
