// React
import { Link } from 'react-router-dom';
// Styles
import '../stylesheets/Login.scss';

const Login = (props) => {
  return (
    <>
      <div>LOGIN</div>
    </>
  );
};

export default Login;
